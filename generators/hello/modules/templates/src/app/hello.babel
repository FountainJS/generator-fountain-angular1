export const hello = {
<% if (modules === 'systemjs') { -%>
  templateUrl: 'app/hello.html',
<% } else { -%>
  template: require('./hello.html'),
<% } -%>
  controller() {
    this.hello = 'Hello World!';
  }
};
