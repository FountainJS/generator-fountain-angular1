<% if (modules !== 'inject') { -%>
import angular from 'angular';

import { hello } from './app/hello.component';

<% if (modules === 'webpack') { -%>
import './index.<%- css %>';

<% } -%>
export const app = 'app';

angular
  .module(app, [])
  .component('app', hello);
<% } else { -%>
angular
  .module('app', []);
<% } -%>
